<h1><%=@song.title%>by<%=@song.artist_name%><h1>

  <% if @song.released? %>
      <h2>Released in <%= @song.release_year %></h2>
  <% end %>

    <h2>Genre:</h2>
    <%= @song.genre %>


<%= link_to 'Edit', song_path(@song) %>
<%= link_to 'Delete', song, method: :delete, data: { confirm: 'For real?' } %>
